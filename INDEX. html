Got it 👍 You want a Digital Portfolio built with HTML, CSS, and JavaScript — with the files separated and using a unique, modern design.

Here’s a clean example:


---

📂 File 1: index.html

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My Digital Portfolio</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <!-- Navigation -->
  <header>
    <nav>
      <h1 class="logo">MyPortfolio</h1>
      <ul class="nav-links">
        <li><a href="#about">About</a></li>
        <li><a href="#projects">Projects</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
      <button class="theme-toggle">🌙</button>
    </nav>
  </header>

  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-text">
      <h2>Hello, I'm <span class="highlight">Gokul</span></h2>
      <p>A passionate web developer who loves creating interactive experiences.</p>
      <a href="#projects" class="btn">View My Work</a>
    </div>
  </section>

  <!-- About Section -->
  <section id="about" class="about">
    <h2>About Me</h2>
    <p>
      I’m a web developer skilled in HTML, CSS, JavaScript, and PHP. I enjoy turning 
      creative ideas into beautiful and functional web applications.
    </p>
  </section>

  <!-- Projects Section -->
  <section id="projects" class="projects">
    <h2>Projects</h2>
    <div class="project-grid">
      <div class="project-card">
        <h3>Student Management System</h3>
        <p>A PHP & MySQL project to manage student records efficiently.</p>
      </div>
      <div class="project-card">
        <h3>Portfolio Website</h3>
        <p>A responsive portfolio to showcase my skills and projects.</p>
      </div>
      <div class="project-card">
        <h3>To-Do App</h3>
        <p>A simple task manager built using JavaScript and local storage.</p>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section id="contact" class="contact">
    <h2>Contact Me</h2>
    <form id="contactForm">
      <input type="text" placeholder="Your Name" required />
      <input type="email" placeholder="Your Email" required />
      <textarea placeholder="Your Message" required></textarea>
      <button type="submit" class="btn">Send</button>
    </form>
  </section>

  <footer>
    <p>&copy; 2025 Gokul. All Rights Reserved.</p>
  </footer>

  <script src="script.js"></script>
</body>
</html>


---

📂 File 2: style.css

/* General Styles */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
body {
  font-family: 'Poppins', sans-serif;
  line-height: 1.6;
  background: #f9f9f9;
  color: #333;
  transition: background 0.3s, color 0.3s;
}
h1, h2, h3 {
  margin-bottom: 1rem;
}
a {
  text-decoration: none;
  color: inherit;
}

/* Navigation */
header {
  background: #222;
  color: #fff;
  padding: 1rem 2rem;
  position: sticky;
  top: 0;
  z-index: 1000;
}
nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.logo {
  font-size: 1.5rem;
  font-weight: bold;
}
.nav-links {
  display: flex;
  gap: 1.5rem;
  list-style: none;
}
.nav-links a {
  color: #fff;
  transition: color 0.3s;
}
.nav-links a:hover {
  color: #ff6f61;
}
.theme-toggle {
  background: none;
  border: none;
  color: #fff;
  font-size: 1.2rem;
  cursor: pointer;
}

/* Hero */
.hero {
  height: 90vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  background: linear-gradient(to right, #ff6f61, #ff9966);
  color: #fff;
}
.hero h2 {
  font-size: 2.5rem;
}
.highlight {
  color: #ffd700;
}
.btn {
  display: inline-block;
  margin-top: 1rem;
  padding: 0.7rem 1.5rem;
  background: #333;
  color: #fff;
  border-radius: 25px;
  transition: 0.3s;
}
.btn:hover {
  background: #ff6f61;
}

/* About */
.about, .projects, .contact {
  padding: 4rem 2rem;
  text-align: center;
}
.about p {
  max-width: 600px;
  margin: 0 auto;
}

/* Projects */
.project-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1.5rem;
  margin-top: 2rem;
}
.project-card {
  background: #fff;
  padding: 1.5rem;
  border-radius: 15px;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
  transition: transform 0.3s;
}
.project-card:hover {
  transform: translateY(-5px);
}

/* Contact */
form {
  max-width: 500px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}
form input, form textarea {
  padding: 0.8rem;
  border: 1px solid #ccc;
  border-radius: 10px;
  outline: none;
}
form button {
  align-self: center;
}

/* Footer */
footer {
  background: #222;
  color: #fff;
  padding: 1rem;
  text-align: center;
  margin-top: 2rem;
}

/* Dark Mode */
body.dark {
  background: #121212;
  color: #f0f0f0;
}
body.dark header {
  background: #000;
}
body.dark .btn {
  background: #ff6f61;
}
body.dark .project-card {
  background: #1e1e1e;
}


---

📂 File 3: script.js

// Theme Toggle (Light/Dark Mode)
const toggleBtn = document.querySelector('.theme-toggle');
const body = document.body;

toggleBtn.addEventListener('click', () => {
  body.classList.toggle('dark');
  toggleBtn.textContent = body.classList.contains('dark') ? "☀️" : "🌙";
});

// Contact Form Submission (Demo)
const contactForm = document.getElementById('contactForm');
contactForm.addEventListener('submit', (e) => {
  e.preventDefault();
  alert("Thank you! Your message has been sent.");
  contactForm.reset();
});


---

✨ Features in this design:

Responsive navigation bar with sticky header.

Hero section with gradient background.

Project cards with hover animation.

Dark/Light mode toggle using JavaScript.

Contact form with alert on submit.



---

Do you want me to make this portfolio single-page (scroll sections only) or multi-page (separate pages for About, Projects, Contact)?

